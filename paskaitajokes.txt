Why did the Python programmer import a file?
To get to the other side of the code, of course!

Why did the file refuse to import into Python?
Because it had a case of stage fright.

Why did the Python script cross the road?
To import a file from the other side.

Why was the Python programmer always cold?
Because they kept importing the wrong files!